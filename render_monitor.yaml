services:
  - type: web
    name: ventassccrn
    env: node
    buildCommand: "npm install --production=false"
    startCommand: "node server.js"
    repo: https://github.com/hfigueroa2018/ventassccrn
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        fromDatabase:
          name: monitor_pro
          property: connectionString
      - key: NPM_CONFIG_PRODUCTION
        value: "false"

databases:
  - name: monitor_pro
    databaseName: monitor_pro
    user: monitor_pro_user
    plan: free
    version: "17"